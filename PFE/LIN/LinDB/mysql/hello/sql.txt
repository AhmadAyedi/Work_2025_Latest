CREATE DATABASE lin_data2;

CREATE TABLE lin_data2.messages (
    id INT AUTO_INCREMENT PRIMARY KEY,
    message_id INT,
    message VARCHAR(255),
    timestamp TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);



CREATE USER 'root'@'10.20.0.13' IDENTIFIED BY 'root';
GRANT ALL PRIVILEGES ON *.* TO 'root'@'10.20.0.13' WITH GRANT OPTION;
FLUSH PRIVILEGES;

